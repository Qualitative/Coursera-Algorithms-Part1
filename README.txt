Project with solutions for tasks in Algorithms Part 1 Coursera's training

Each folder is a separate Java Project (for convenience the build.gradle is added to simplify import of projects as "Gradle Project")